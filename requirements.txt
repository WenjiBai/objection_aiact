# Core (A uses)
pydantic>=2.6
pyyaml>=6.0
anthropic>=0.39
python-dotenv>=1.0

# RAG (B uses; listed here so single `pip install -r` sets up everyone)
chromadb>=0.5
sentence-transformers>=3.0

# Frontend (B uses)
streamlit>=1.40

# Dev only
pytest>=8.0
